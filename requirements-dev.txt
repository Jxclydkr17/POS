# requirements-dev.txt — Dependencias de DESARROLLO y BUILD
#
# FASE 5 — Fix 5.3 / 5.4: Herramientas de test y build separadas.
# NO incluir en el .exe de producción.
#
# Instalar: pip install -r requirements-dev.txt

# ── Incluir dependencias de producción ──
-r requirements.txt

# ── Testing ──
pytest==8.3.4
iniconfig==2.3.0
pluggy==1.6.0

# ── Build / Packaging ──
pyinstaller==6.11.1
pyinstaller-hooks-contrib==2026.4
altgraph==0.17.5

# ── Windows-specific build tools ──
# Solo necesarios en Windows para PyInstaller
pefile==2023.2.7
pywin32-ctypes==0.2.3