# requirements.txt — Dependencias de PRODUCCIÓN
#
# FASE 5 — Fix 5.3 / 5.4: Separar dependencias de producción vs desarrollo.
# pytest, pyinstaller y herramientas de build van en requirements-dev.txt.
#
# FASE 2 — Fix 2.1: python-jose reemplazado por PyJWT.
# FASE 2 — Fix 2.5 (cerrado): python-escpos + pyusb para impresión térmica.
#
# Instalar: pip install -r requirements.txt

# ── Framework web ──
fastapi==0.115.6
uvicorn==0.34.0
starlette==0.41.3
python-multipart==0.0.20
h11==0.16.0
httptools==0.7.1
websockets==16.0
watchfiles==1.1.1

# ── Base de datos ──
SQLAlchemy==2.0.36
alembic==1.14.1
PyMySQL==1.1.1
greenlet==3.3.2

# ── Validación / Schemas ──
pydantic==2.10.4
pydantic-settings==2.7.1
pydantic_core==2.27.2
annotated-types==0.7.0
email-validator==2.3.0
typing_extensions==4.15.0

# ── Seguridad / Auth ──
PyJWT==2.9.0
# Fix 3.3: passlib eliminado (abandonado, DeprecationWarning en Python 3.12+).
# Se usa bcrypt directamente — compatible con hashes existentes ($2b$).
bcrypt==4.0.1
cryptography==44.0.0

# ── HTTP client ──
requests==2.33.1
httpx==0.28.1
httpcore==1.0.9
certifi==2026.2.25
charset-normalizer==3.4.7
urllib3==2.6.3
idna==3.11

# ── UI (PySide6) ──
PySide6==6.8.1
PySide6_Addons==6.8.1
PySide6_Essentials==6.8.1
shiboken6==6.8.1

# ── PDF / Reportes ──
reportlab==4.2.5
qrcode==8.0
pillow==12.2.0

# ── Excel / CSV ──
# FASE 3 — Fix 3.2: pandas y numpy eliminados (~100MB menos en el .exe).
# Todas las exportaciones Excel ahora usan openpyxl puro.
openpyxl==3.1.5
et_xmlfile==2.0.0

# ── XML (factura electrónica) ──
lxml==5.3.0

# ── Email ──
yagmail==0.15.293
premailer==3.10.0
cssselect==1.4.0
cssutils==2.11.1

# ── Gráficos ──
# NOTA: matplotlib se usa para gráficos interactivos embebidos en Qt
# (daily_report_view, financial_view, sales/purchases_analytics_view,
# performance_chart_card). No es sustituible por reportlab porque
# son gráficos renderizados en vivo en la UI, no solo en PDFs.
matplotlib==3.10.0
contourpy==1.3.3
cycler==0.12.1
fonttools==4.62.1
kiwisolver==1.5.0
pyparsing==3.3.2

# ── Impresión térmica ESC/POS (Fix 2.5 cerrado) ──
# python-escpos genera comandos ESC/POS y maneja los transportes de
# red (sock TCP), serie y USB. Trae su propio escpos-printer-db con
# perfiles para impresoras Epson, Bixolon, Star, etc.
#
# pyusb es OPCIONAL: solo se necesita si `printer_type='usb'` en
# Settings. Para impresoras en red (`printer_type='network'`) y para
# impresión vía sistema operativo (PDF al spool de Windows/macOS/
# Linux) NO se requiere pyusb. Lo incluimos para que los usuarios
# con impresora USB no tengan que instalar nada manualmente; en
# Linux puede requerir además libusb del sistema y reglas udev.
python-escpos==3.1
pyusb==1.2.1

# ── Utilidades ──
python-dotenv==1.0.1
python-dateutil==2.9.0.post0
pytz==2026.1.post1
tzdata==2026.1
six==1.17.0
click==8.3.1
colorama==0.4.6
Mako==1.3.10
MarkupSafe==3.0.3
packaging==26.0
more-itertools==11.0.1
PyYAML==6.0.3
cffi==2.0.0
pycparser==3.0
pyasn1==0.6.3
dnspython==2.8.0
rsa==4.9.1
ecdsa==0.19.2
cachetools==7.0.5
chardet==7.4.0.post2
anyio==4.13.0